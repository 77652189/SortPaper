# 2026-04-29 工作记录

## 修复 PaperSort 项目潜在问题

### 修复内容

1. **main.py** - 修复 verdicts 字段不存在问题
   - 原代码使用 `state.get("verdicts", [])`，但 PipelineState 没有 `verdicts` 字段
   - 修复：合并 `text_verdicts` + `table_verdicts` + `image_verdicts` 进行统计
   - 同时修复了 `retries` 字段，改为 `text_retries`/`table_retries`/`image_retries`

2. **pipeline_graph.py** - 修复 final_route 返回类型
   - 原返回类型 `Literal["store", "finalize_failed"]` 不包含 worker 节点
   - 修复：`Literal["store", "finalize_failed", "text_worker", "table_worker", "image_worker"]`

3. **pipeline_graph.py** - 删除重复的 JudgeVerdict 定义
   - 原文件定义了 `JudgeVerdict(TypedDict)`，与 `llm_judge.py` 中的 dataclass 重复
   - 修复：改为 `from src.judge.llm_judge import JudgeVerdict, LLMJudge`
   - 同时将 PipelineState 中的类型注解改为 `list[dict]`（实际存储的是字典）

4. **llm_judge.py** - 添加 API 错误处理
   - `judge()` 方法：添加 try-except 包裹 API 调用，检查 response.code，检查 response.output 是否为空
   - `summarize()` 方法：同样的错误处理

5. **vision_parser.py** - 添加 API 错误处理
   - `_describe_image()` 方法：添加 try-except 包裹 API 调用，检查错误响应，返回友好错误信息而非崩溃

6. **faiss_store.py** - 修复 embedding 维度问题
   - 原代码硬编码 `dimension=1024`，但 `text-embedding-v3` 可能返回 1536 维
   - 修复：懒创建 index，`__init__` 中 `self.index = None`，首次 `embed()` 时根据实际维度创建

### 剩余问题（未修复）

- README.md 内容太简略，缺少使用说明
- 测试覆盖率低，仅覆盖了 LayoutDeduplicator
- 重试循环的工作流连接需要验证（LangGraph 条件边的实际行为）
