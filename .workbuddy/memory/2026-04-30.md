# 2026-04-30 工作日志

## pipeline_graph.py 语法错误修复

用户指出上一轮修改引入的三处语法错误，已全部修复：

1. **judge_table_node 第306行** — `chunks = state.get("table_chunks", []))` 多了一个右括号 → 删除多余 `)`
2. **judge_image_node 第325行** — `chunks = state.get("image_chunks", []))` 多了一个右括号 → 删除多余 `)`
3. **judge_image_node 第340行** — `"routes_done": {"image"}"}` 多了一个引号 → 改为 `"routes_done": {"image"}}`

lint 确认 0 错误。

## 经验

- 用 `replace_in_file` 批量修改时，字符串匹配容易引入多余符号（括号、引号），每次修改后必须跑 lint 或肉眼检查关键行。
- 集合字面量 `{""}` 的闭合括号是 `}}`（因为在外层字典里），容易写成 `"}` 导致语法错误。
